"""Placeholder module so the package is non-empty before proto generation runs.

TODO(M0): delete this once ``scripts/gen-proto.sh`` produces real modules.
"""

from __future__ import annotations

PROTO_VERSION: str = "v1"
"""Proto package version this stub set targets (``corlinman.v1``)."""
