# corlinman-providers

LLM provider adapters: Anthropic, OpenAI, Google, plus the China bucket
(DeepSeek, Qwen, GLM). Also hosts the ``CorlinmanError`` hierarchy used by
the agent-client on the Rust side for ``FailoverReason`` classification.
